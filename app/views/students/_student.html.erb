<div id="<%= dom_id student %>">
  <div>
    <h1><%= student.full_name %></h1>
    <h2><%= "#{student.email} - #{student.phone}"%></h2>
  </div>
  
  <%= link_to "Waltz", charts_path(student.id, "Waltz") %>