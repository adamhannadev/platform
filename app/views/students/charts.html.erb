<div>
    <table>
      <thead>
        <th></th>
        <th>LEVEL</th>
        <th>FIGURE</th>
        <th>T</th>
        <th>M</th>
        <th>P</th>
      </thead>
      <tbody>
        <% grouped = @charts.group_by { |c| c.figure.level } %>
        <% grouped.each do |level, charts| %>
          <% charts.each_with_index do |c, i| %>
<<<<<<< HEAD
            <tr>
              <% if i == 0 %>
                <td rowspan="<%= charts.size %>" style="writing-mode: vertical-rl; font-weight: bold; background: #e0e7ef;"><%= level %></td>
              <% end %>
              <td><%= c.figure.number %></td>
=======
            <tr class="<%= 'core' if c.figure.core %>">
              <% if i == 0 %>
                <td rowspan="<%= charts.size %>" style="writing-mode: vertical-lr; font-weight: bold; background: #e0e7ef; text-align:center;"><%= level.upcase %></td>
              <% end %>
              <td style="<%= 'text-align:right;' unless c.figure.core %>"><%= c.figure.number %></td>
>>>>>>> syllabus
              <td><%= c.figure.name %></td>
              <td class="toggle-cell" data-chart-id="<%= c.id %>" data-field="timing"><%= "X" if c.timing %></td>
              <td class="toggle-cell" data-chart-id="<%= c.id %>" data-field="movement"><%= "X" if c.movement %></td>
              <td class="toggle-cell" data-chart-id="<%= c.id %>" data-field="partnering"><%= "X" if c.partnering %></td>
            </tr>
          <% end %>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<script>
document.querySelectorAll('.toggle-cell').forEach(function(td) {
  td.addEventListener('click', function() {
    const chartId = td.dataset.chartId;
    const field = td.dataset.field;
    const current = td.textContent.trim() === "X";
    // Toggle display
    td.textContent = current ? "" : "X";
    // Send AJAX request
    fetch(`/charts/${chartId}/toggle`, {
      method: "PATCH",
      headers: {
        "Content-Type": "application/json",
        "X-CSRF-Token": document.querySelector('meta[name="csrf-token"]').content
      },
      body: JSON.stringify({ field: field, value: !current })
    });
  });
});
</script>

<style>
  table {
    border-collapse: collapse;
    width: 50%;
    margin: 1em auto 0 auto;
    background: #fafafa;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    border-radius: 8px;
    overflow: hidden;
  }
  th, td {
    border: 1px solid #bbb;
    padding: 0.75em 1em;
    font-size: 1em;
    transition: background 0.2s;
  }
  th {
    background: #e0e7ef;
    font-weight: 600;
    color: #333;
  }
  tr:hover td {
    background: #dbeafe;
  }
  .toggle-cell {
    cursor: pointer;
    font-weight: bold;
    color: #2563eb;
    background: #f3f4f6;
    border-radius: 4px;
    width: 2em;
    text-align:center;
  }
  .toggle-cell:hover {
    background: #bfdbfe;
    color: #1e40af;
  }
  .core {
    font-weight: 900;
    background-color: aliceblue;
    text-align: center;
  }
</style>